name: imdclient-test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - numpy

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - MDAnalysis
  - MDAnalysisTests
  - docker-py

